import { OnInit, EventEmitter } from '@angular/core';
import { Polygon } from '../../types';
export declare class SvgPolygonComponent implements OnInit {
    private pointsString;
    constructor();
    polygon: Polygon;
    polygonMousedown: EventEmitter<{
        mouseEvent: MouseEvent;
        polygon: Polygon;
    }>;
    ngOnInit(): void;
    private getPolyPoints(points);
    onPolygonMouseDown($event: any): void;
}
