import { OnInit, EventEmitter } from '@angular/core';
import { Polyline } from '../../types';
export declare class SvgPolylineComponent implements OnInit {
    private pointsString;
    constructor();
    polyline: Polyline;
    polylineMousedown: EventEmitter<{
        mouseEvent: MouseEvent;
        polyline: Polyline;
    }>;
    ngOnInit(): void;
    private getPolyPoints(points);
    onPolylineMouseDown(event: any): void;
}
